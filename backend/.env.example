# Sample environment configuration for Quant UI backend
# Copy to .env and adjust paths for your local setup.

# Optional: absolute path to external quant research engine root (sources, modules)
# Supports either QUANT_CORE_ROOT or CORE_ROOT. QUANT_CORE_ROOT takes precedence.
QUANT_CORE_ROOT=C:/dev/quant
# CORE_ROOT=C:/dev/quant

# Data directories (curated & raw)
QUANT_DATA_ROOT=./data_curated
QUANT_RAW_ROOT=./data_raw

# SQLite DB path
QUANT_DB_PATH=./runs.sqlite

# Optional API key (if auth added later)
# QUANT_API_KEY=devkey

# Broker mode override (dummy, paper, live)
BROKER_MODE=dummy
